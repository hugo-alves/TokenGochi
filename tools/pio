#!/usr/bin/env sh
set -eu

UVX_BIN="$(command -v uvx || true)"
if [ -z "$UVX_BIN" ]; then
  echo "error: uvx not found; install uv from https://docs.astral.sh/uv/" >&2
  exit 1
fi

exec "$UVX_BIN" --python 3.12 --with intelhex platformio "$@"
